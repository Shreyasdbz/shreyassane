import SectionContainer from '../base/SectionContainer'

const ErrorSection = () => {
  return (
    <SectionContainer>
      <p>Sorry :(</p>
      <p>It looks like data couldn't be loaded</p>
    </SectionContainer>
  )
}

export default ErrorSection
